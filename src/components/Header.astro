<header>
  <div id="albarregasImage">
    <img
      src="images/header/logoAlbarregas.png"
      alt="Logo del Instituto Albarregas"
    />
  </div>
  <div id="celebrationDays">
    <p>
      <span>3</span>|<span>4</span>|<span>5</span>|<span>6</span>|<span>7</span
      >&nbsp;de Febrero
    </p>
    <h5>IES Albarregas</h5>
  </div>
  <div id="whiteBar"></div>
  <div id="webParts">
    <span>Inicio</span>
    <span>Familias profesionales</span>
    <span>Colaboradores</span>
  </div>
</header>

<style>

  :root {
    --backgroundColor: #090ab2;
    --colorFont: #ffff;
    --hoverColor: #ffbe17;
  }

  header {
    width: 100%;
    height: 120px;
    background: var(--backgroundColor);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    color: var(--colorFont);
    font-family: Verdana;
    padding: 0.5rem;
    position: absolute;
    top: 0;
    z-index: 2;
    box-shadow: rgb(72 72 72 / 35%) 0px 5px 15px;
    overflow: hidden;
  }

  #albarregasImage {
    padding: 1rem;
    width: 20%;
  }

  #albarregasImage > img {
    width: 100%;
    filter: invert(100%);
  }

  #celebrationDays {
    width: 30%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    font-size: 1.5vw;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  h5 {
    font-size: 2vw;
  }

  #whiteBar {
    width: 100px;
    rotate: 90deg;
    height: 1%;
    background: var(--colorFont);
    border: solid 1px var(--colorFont);
  }

  #webParts {
    width: 45%;
    display: flex;
    align-items: center;
    justify-content: space-around;
    font-size: 1.5vw;
  }

  #webParts span {
    position: relative;
    cursor: pointer;
    transition:
      color 0.3s ease,
      transform 0.3s ease;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  #webParts span:hover {
    color: var(--hoverColor);
    transform: scale(1.1);
  }

  #webParts span:hover::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 100%;
    height: 3px;
    background-color: var(--hoverColor);
    transition: width 0.3s ease;
  }

  #webParts span::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 3px;
    background-color: var(--hoverColor);
    transition: width 0.3s ease;
  }

  @keyframes fallDown {
    0% {
      opacity: 0;
      transform: translateY(-120%);
    }

    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideFromLeft {
    0% {
      opacity: 0;
      transform: translateX(-150px);
    }

    100% {
      opacity: 1;
      transform: translateX(0);
    }
  }

  #celebrationDays p {
    display: flex;
    align-items: center;
  }

  #celebrationDays p span {
    display: inline-block;
    opacity: 0;
  }

  #celebrationDays p span:nth-child(1) {
    animation: fallDown 0.8s ease-out 0.3s forwards;
  }

  #celebrationDays p span:nth-child(2) {
    animation: fallDown 0.8s ease-out 0.6s forwards;
  }

  #celebrationDays p span:nth-child(3) {
    animation: fallDown 0.8s ease-out 0.9s forwards;
  }

  #celebrationDays p span:nth-child(4) {
    animation: fallDown 0.8s ease-out 1.2s forwards;
  }

  #celebrationDays p span:nth-child(5) {
    animation: fallDown 0.8s ease-out 1.5s forwards;
  }

  #webParts span:nth-child(1) {
    animation: slideFromLeft 0.8s ease-out 0.3s forwards;
    opacity: 0;
  }

  #webParts span:nth-child(2) {
    animation: slideFromLeft 0.8s ease-out 0.6s forwards;
    opacity: 0;
  }

  #webParts span:nth-child(3) {
    animation: slideFromLeft 0.8s ease-out 0.9s forwards;
    opacity: 0;
  }

  /* Media Queries para adaptarse a diferentes tamaños de pantalla */
  /* 414px x 896px (móvil grande) */
  @media screen and (width < 1440px) and (width >= 414px) {
    header {
      height: 100px;
    }

    #albarregasImage {
      display: none;
    }

    #celebrationDays {
      width: 30%;
      font-size: 2vw;
    }

    #whiteBar {
      width: 80px;
    }

    h5 {
      font-size: 2.5vw;
    }

    #webParts {
      width: 70%;
      font-size: 2.5vw;
    }

    #webParts span:first-child {
      display: none;
    }

    #webParts span:hover::after {
      height: 2px;
    }

    #webParts span::after {
      height: 2px;
    }

    @keyframes slideFromLeft {
      0% {
        opacity: 0;
        transform: translateX(-50px);
      }

      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }
  }

  /* 360px x 640px (móvil pequeño) */
  @media screen and (width < 414px) {
    header {
      height: 60px;
    }

    #albarregasImage {
      display: none;
    }

    #celebrationDays {
      width: 30%;
      font-size: 2vw;
    }

    #whiteBar {
      width: 40px;
    }

    h5 {
      font-size: 2.5vw;
    }

    #webParts {
      width: 70%;
      font-size: 2.5vw;
    }

    #webParts span:first-child {
      display: none;
    }

    #webParts span:hover::after {
      height: 1px;
    }

    #webParts span::after {
      height: 1px;
    }

    @keyframes slideFromLeft {
      0% {
        opacity: 0;
        transform: translateX(-10px);
      }

      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }
  }
</style>
